
#!/bin/bash

# this script will setup bike_env and pip install dependencies from requirements.txt
# first make this setup script executable 
# > chmod +ugo+x setup_venv


echo "creating bikenv virtual environment"
virtualenv bikenv

echo "activated the new env"
source bikenv/bin/activate

echo "installing dependecies from requirements.txt"
pip install -r requirements.txt
